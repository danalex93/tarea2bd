@model IEnumerable<Phoro.Models.Comentario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tema.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.text)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tema.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.text)
        </td>
        <td>
            @if (ViewBag.show_edit) {
                @Html.ActionLink("Edit", "Edit", new { id=item.id_comentario })
            }
            @if (ViewBag.show_details) {
                @Html.ActionLink("Details", "Details", new { id=item.id_comentario })
            }
            @if (ViewBag.show_delete) {
                @Html.ActionLink("Delete", "Delete", new { id=item.id_comentario })
            }
        </td>
    </tr>
}

</table>
